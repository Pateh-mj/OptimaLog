APP_NAME=ExpedientLog
APP_ENV=production
APP_DEBUG=false
APP_TIMEZONE=Africa/Lusaka
APP_URL=http://localhost/ExpedientLog

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=exp_log
DB_USER=postgres
DB_PASS=

# Storage: Render filesystem is ephemeral — use an object storage for persistent uploads
# S3_BUCKET=your-bucket
# S3_KEY=...
# S3_SECRET=...
# S3_REGION=...
